NODE_ENV=development
# Port number
PORT=5020

DB_HOST=13.206.201.182
DB_USER=postgres
DB_PASSWORD=Iisl@2026
DB_NAME=Via-Exam

# CORS Configuration
# Local:
# CORS_ORIGIN=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001
# FRONTEND_URL=http://localhost:3000
# Server (13.206.201.182) — frontend runs on port 3002:
CORS_ORIGIN=http://13.206.201.182:3002,http://13.206.201.182
FRONTEND_URL=http://13.206.201.182:3002

JWT_SECRET=thisisasamplesecret
# Number of minutes after which an access token expires
JWT_ACCESS_EXPIRATION_MINUTES=30
# Number of days after which a refresh token expires
JWT_REFRESH_EXPIRATION_DAYS=30
# Number of minutes after which a reset password token expires
JWT_RESET_PASSWORD_EXPIRATION_MINUTES=10
# Number of minutes after which a verify email token expires
JWT_VERIFY_EMAIL_EXPIRATION_MINUTES=10

# SMTP configuration options for the email service
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USERNAME=rufiadah.innoblooms@gmail.com
SMTP_PASSWORD=iqmm vflb gsoh cumi
EMAIL_FROM=rufiadah.innoblooms@gmail.com

SECRET_KEY=sbipayment_cipl
SECRET_IV=1234567890123456
ECNRYPTION_METHOD=aes-256-cbc

# Auth Secrets
ACCESS_SECRET=noah2024
REFRESH_SECRET=boolean2420

# ── Python services (two separate servers) ─────────────────────────────────────
# Full base URL = http://<ip>:<port>  (no trailing slash, no path)

# OCR server — ocr_server.py → uvicorn ocr_server:app --port 8000   (POST /ocrOutput)
# Local:
# OCR_SERVICE_URL=http://localhost:8000
OCR_SERVICE_URL=http://13.206.201.182:8000

# Evaluation server — pipeline6.py → uvicorn pipeline6:app --port 8006
# (POST /evaluate-text, /preprocess-exam, /visual-pre-eval)
# Local:
# EVAL_SERVICE_URL=http://localhost:8006
EVAL_SERVICE_URL=http://13.206.201.182:8006

# Optional per-endpoint overrides (only if one endpoint lives somewhere else):
# OCR_API_URL=http://<ip>:8000/ocrOutput
# OCR_PIPELINE_URL=http://<ip>:8006/evaluate-text
# OCRNEW5_PIPELINE_URL=http://<ip>:8006/evaluate-text
# OCR6_PIPELINE_URL=http://<ip>:8006/evaluate-text
# PIPELINE6_PREPROCESS_URL=http://<ip>:8006/preprocess-exam
# PIPELINE6_VISUAL_PREEVAL_URL=http://<ip>:8006/visual-pre-eval
# Legacy /evaluate route only (defaults to the evaluation host on port 8002):
# EVALUATION_API_URL=http://<ip>:8002/evaluation

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=dmefqfwff
CLOUDINARY_API_KEY=846656458798157
CLOUDINARY_API_SECRET=v8_RPu4RhQti0hgUq7_d6664mlQ

